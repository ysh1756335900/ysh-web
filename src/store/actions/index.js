import { login, register, logout, goto_404 } from './auth';
import { saveUserInfo, clearUserInfo } from './user';

export {
    login,
    register,
    logout,
    saveUserInfo,
    clearUserInfo,
    goto_404
}